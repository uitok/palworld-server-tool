# 更新说明（2026-03-20）

> 本次提交用于同步当前仓库已经完成的工程治理、后端增强、前端工作台与测试补强结果。
> `docs/development/` 下的阶段计划与路线图文档暂不随本次提交推送。

## 本次同步范围

### 1. 工程与配置治理

- 统一并补强构建链路与脚本
- 补充开发/Agent/Docker/K8s 配置样例
- 增强启动前配置校验与错误早失败能力

### 2. 后端能力增强

- 统一 API 返回模型与部分错误处理
- 加固存档来源解析、同步与备份链路
- 补充任务状态观测与诊断能力
- 扩展玩家聚合、批量管理与 PalDefender 审计/重试能力

### 3. 前端工作台更新

- 新增 `/ops` 运维总览相关能力
- 新增 `/players` 玩家工作台
- 新增 `/paldefender` 独立工作台
- 批量发放面板支持审计筛选、导出与失败重试

### 4. 测试与验证

- 新增多组 `api/`、`service/`、`internal/` 相关测试
- 已完成 `go test ./...`
- 已完成 `pnpm --dir web exec vite build --outDir /tmp/pst-web-build-p6`

## 说明

本次推送刻意不包含以下本地内容：

- `docs/development/` 下的计划、阶段说明与路线图文档
- 本地分析文件与构建产物目录
- 临时缓存与本地二进制文件

后续如需单独同步开发计划文档，可在下一次文档提交中独立整理后再推送。
